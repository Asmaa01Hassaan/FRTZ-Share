<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        
        <!-- ========================================== -->
        <!-- CRM.LEAD (OPPORTUNITIES) RULES              -->
        <!-- ========================================== -->
        
        <!-- Rule 1: BD User - Own Opportunities Only -->
        <record id="crm_lead_rule_user_own_only" model="ir.rule">
            <field name="name">Laft: CRM - User sees own only</field>
            <field name="model_id" ref="crm.model_crm_lead"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('group_bd_user_own'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Rule 2: BD Manager - Team Opportunities -->
        <record id="crm_lead_rule_manager_team" model="ir.rule">
            <field name="name">Laft: CRM - Manager sees team</field>
            <field name="model_id" ref="crm.model_crm_lead"/>
            <field name="domain_force">[('team_id.member_ids', 'in', [user.id])]</field>
            <field name="groups" eval="[(4, ref('group_bd_manager_team'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Rule 3: BD Executive - All Opportunities -->
        <record id="crm_lead_rule_executive_all" model="ir.rule">
            <field name="name">Laft: CRM - Executive sees all</field>
            <field name="model_id" ref="crm.model_crm_lead"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_bd_executive'))]"/>
        </record>

    </data>
</odoo>

